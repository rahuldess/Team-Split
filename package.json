{
  "name": "teamsplit",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.rahulsambari.dev/teamsplit",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3",
    "@material-ui/core": "^4.11.0",
    "@material-ui/icons": "^4.9.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "copy": "cp ./CNAME ./build/CNAME",
    "eject": "react-scripts eject",
    "predeploy": "npm run build && yarn copy",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
